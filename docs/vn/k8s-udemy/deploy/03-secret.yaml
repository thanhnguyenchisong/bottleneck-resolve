apiVersion: v1
kind: Secret
metadata:
  name: bottleneck-resolve-secret
  namespace: bottleneck-resolve
  labels:
    app: bottleneck-resolve
type: Opaque
stringData:
  # Example: Add your secrets here
  # database-password: "your-password"
  # api-key: "your-api-key"
  # Note: In production, use sealed-secrets, external-secrets, or vault
  # This is a template - update with actual secrets
